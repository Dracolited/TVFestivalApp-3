dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter()
        maven { url 'https://repo.brightcove.com/releases' }

    }
}
rootProject.name = "TVFestivalApp"
include ':app'
